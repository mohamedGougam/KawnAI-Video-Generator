# Kawn Video Generation — Python backend
#
# Docker: PyTorch CPU wheels are installed in backend/Dockerfile before this file.
# Local GPU: install torch from https://pytorch.org then pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
numpy>=1.26.0
Pillow>=10.0.0

diffusers>=0.31.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.0

# Optional: Redis job queue (set REDIS_URL). API enqueues; ARQ worker runs inference.
arq>=0.26.0
redis>=5.0.0,<6.0.0
